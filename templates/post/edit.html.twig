{% extends 'base.html.twig' %}

{% block title %}Ajouter un article{% endblock %}

{% block body %}
    <h2>Editer un article</h2>

    <div class="mb-3 ">
        <a href="{{ path('post_index') }}" class="btn btn-outline-secondary btn-sm">Retour</a>
    </div>

    {{ form_start(form) }}
    {{ form_rest(form) }}
    <div class="text-end">
        <button class="btn btn-outline-primary">Sauvegarder</button>
    </div>
    {{ form_end(form) }}

{% endblock %}
